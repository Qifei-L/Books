@page "/reports/profit-loss"
@rendermode InteractiveServer
@inject GeneralLedgerAppService Gl
@inject ReportService ReportsService
@inject ReportDownloadService Downloads

<div class="page-header">
    <div>
        <p class="eyebrow">Reports</p>
        <h1>Profit & Loss <span class="plum-mark" aria-hidden="true">✣</span></h1>
    </div>
    <div class="report-actions">
        <button class="secondary export-button" disabled="@IsExporting" @onclick="ExportExcel">
            <span class="export-icon excel-icon">XLS</span>
            @(exportingFormat == "excel" ? "Exporting..." : "Export Excel")
        </button>
        <button class="secondary export-button" disabled="@IsExporting" @onclick="ExportPdf">
            <span class="export-icon pdf-icon">PDF</span>
            @(exportingFormat == "pdf" ? "Exporting..." : "Export PDF")
        </button>
        <button class="secondary" @onclick="Load" disabled="@IsExporting">Refresh</button>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(exportError))
{
    <div class="error-note">@exportError</div>
}

<section class="panel">
    @if (loading)
    {
        <div class="empty">Loading profit and loss...</div>
    }
    else
    {
        <table>
            <thead>
                <tr>
                    <th>Account</th>
                    <th>Name</th>
                    <th>Type</th>
                    <th class="amount">Balance</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var row in rows)
                {
                    <tr>
                        <td>@row.AccountCode</td>
                        <td>@row.AccountName</td>
                        <td>@row.AccountType</td>
                        <td class="amount">@row.Balance.ToString("N2")</td>
                    </tr>
                }
            </tbody>
            <tfoot>
                <tr>
                    <th colspan="3">Net Income</th>
                    <th class="amount">@rows.Sum(x => x.AccountType == AccountType.Expense ? -x.Balance : x.Balance).ToString("N2")</th>
                </tr>
            </tfoot>
        </table>
    }
</section>

@code {
    private List<FinancialStatementRowDto> rows = [];
    private bool loading = true;
    private int ledgerId;
    private string? exportingFormat;
    private string? exportError;
    private bool IsExporting => !string.IsNullOrWhiteSpace(exportingFormat);

    protected override async Task OnInitializedAsync() => await Load();

    private async Task Load()
    {
        loading = true;
        var ledger = await Gl.GetDefaultLedgerAsync();
        ledgerId = ledger.Id;
        rows = await ReportsService.GetProfitLossAsync(ledger.Id, null, null);
        loading = false;
    }

    private async Task Export(string format)
    {
        exportingFormat = format;
        exportError = null;
        try
        {
            var contentType = ContentType(format);
            var extension = format == "excel" ? "xlsx" : "pdf";
            await Downloads.DownloadAsync(
                $"/api/v1/reports/profit-and-loss/export?ledgerId={ledgerId}&format={format}",
                $"profit-and-loss-{DateTime.Today:yyyy-MM}.{extension}",
                contentType);
        }
        catch (Exception ex)
        {
            exportError = ex.Message;
        }
        finally
        {
            exportingFormat = null;
        }
    }

    private static string ContentType(string format) =>
        format == "excel" ? ReportDownloadService.ExcelContentType : ReportDownloadService.PdfContentType;

    private Task ExportExcel() => Export("excel");
    private Task ExportPdf() => Export("pdf");
}
